import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.thatcoderagain.chatter',
  appName: 'Focus',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
